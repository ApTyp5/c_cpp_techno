language: c++

addons:
    apt:
        update: true
        packages:
          - cppcheck
          - valgrind
          - cmake
          - libgtest-dev
          - lcov

env:
    - DIR=IT1
    - DIR=IT2
    global:
        CODECOV_TOKEN="01abb78d-7b9d-4213-baec-4d51c880f9bc"

before_script:
    - echo "==========>   Установка gtest   <=========="
    - cd /usr/src/gtest 
    - sudo cmake .
    - sudo make
    - sudo cp *.a /usr/lib
    - cd -
    - cd $DIR

# (На стадии before_script отличный от 0 код возврата остановит билд)
    - echo "==========>   Cppcheck   <=========="
    - cppcheck -j4 --error-exitcode=1 *.cpp *.h

    - echo "==========>   Build   <=========="
    - cmake .
    - cmake --build .
 

script:
    - echo "==========>   Unit testing   <=========="
    - make test.exe

# Расчёт покрытия
    - echo "==========>   Coverage   <=========="

    - cd CMakeFiles/test.exe.dir
    - lcov --directory . --capture --output-file coverage.info
    - lcov --remove coverage.info '/usr/*' "${HOME}"'/.cache/*' --output-file coverage.info
    - lcov --list coverage.info
    - bash <(curl -s https://codecov.io/bash) -f coverage.info
    - cd -


# Проверка утечек памяти тестов
    - valgrind --leak-check=full ./app.exe < func_tests/test1 1>/dev/null 2>1


